name: backend-python-pipeline
run-name:
on:
  push:
    paths:
      - "Backend_Python/**"
    branches:
      - "develop"
      - "main"
jobs:
  backend_python:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Login in DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./Backend_Python
          file: Backend_Python/Dockerfile
          push: True
          tags: |
            yuriichechur/backend_python:${{github.run_number}} 
            yuriichechur/backend_python:latest

  deploy:
    runs-on: ubuntu-latest
    needs: backend_python

    steps:
      - uses: actions/checkout@v4

      - name: Debug SSH key
        run: echo "${{ secrets.SSH_KEY }}" | head -1

      - name: Copy files to production via SSH
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          source: "docker-compose.yml"
          target: /home/ubuntu/FinancialManager/production

      - name: Copy files to staging via SSH
        if: ${{ github.ref == 'refs/heads/develop' }}
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          source: "docker-compose.yml"
          target: /home/ubuntu/FinancialManager/staging

      - name: Deploy to Production
        if: ${{ github.ref == 'refs/heads/main' }}
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd /home/ubuntu/FinancialManager/production
            docker-compose pull backend_python
            docker-compose up backend_python --force-recreate -d

      - name: Deploy to Staging
        if: ${{ github.ref == 'refs/heads/develop' }}
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd /home/ubuntu/FinancialManager/staging
            docker-compose pull backend_python
            docker-compose up backend_python --force-recreate -d
